# nixpacks.toml

[phases.setup]
nixPkgs = ["nodejs", "openssl"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate", "npm run build"]

# IMPORTANTE: startCmd va fuera de las fases, al nivel raíz
startCmd = "npm run start"